# Backend API (apps/server). Default listen: PORT=3000 → http://localhost:3000
# Nuxt dev runs on port 3001 (see nuxt.config.ts devServer.port). The browser loads the UI from :3001;
# RPC, auth, and uploads go to this URL.
NUXT_PUBLIC_SERVER_URL=http://localhost:3000
